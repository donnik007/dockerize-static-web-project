apiVersion: v1
kind: Service
metadata:
    name: "kubedominikklust-6902"
    labels:
        app: "kubedominikklust-6902"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kubedominikklust-6902"